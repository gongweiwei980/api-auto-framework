[pytest]
# 测试文件命名规则
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 用例目录，告诉 pytest 去哪里找
# 如果你直接跑 pytest，它会自动发现当前目录下所有 test_*.py

# 命令行默认参数
addopts = -v --tb=short --html=report.html --self-contained-html

